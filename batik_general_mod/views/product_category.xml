<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_product_category_form_inherit" model="ir.ui.view">
        <field name="name">product.category.form.inherit</field>
        <field name="model">product.category</field>
        <field name="inherit_id" ref="product.product_category_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='parent_id']" position="replace">
                <group col="2">
                    <field name="parent_id"/>
                    <field name="model_code" required="1"/>
                </group>
            </xpath>

            <xpath expr="//group[@name='account_property']" position="after">
                <notebook>
                    <page string="CV">
                        <field name="cv_ids">
                            <tree editable="bottom">
                                <field name="name" column_invisible="True"/>
                                <field name="warehouse_id"/>
                                <field name="cv_id" 
                                    options="{'no_create': True, 'no_edit': True}" 
                                    modifiers="{'readonly': [('warehouse_id', '=', False)]}"
                                    domain="[('id', 'in', product_cv_filter_ids)]"/>
                                <field name="product_cv_filter_ids" column_invisible="True"/>
                            </tree>
                        </field>
                    </page>
                </notebook>
            </xpath>
        </field>
    </record>

</odoo>
